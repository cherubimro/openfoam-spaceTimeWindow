EXE_INC = \
    -I$(LIB_SRC)/finiteVolume/lnInclude \
    -I../../../../lnInclude

# Require libsodium for this utility
ifdef FOAM_USE_SODIUM
    EXE_INC += -DFOAM_USE_SODIUM
endif

EXE_LIBS = \
    -L$(FOAM_USER_LIBBIN) \
    -lspaceTimeWindow

ifdef FOAM_USE_SODIUM
    EXE_LIBS += -lsodium
endif
